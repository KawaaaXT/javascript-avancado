const confirma = Number(prompt('Digite o primeiro valor '))
const confirma2 = Number(prompt('Digite o segundo valor'))


let soma = confirma + confirma2

alert (` A soma entre os 2 valores foram : ${soma} `)